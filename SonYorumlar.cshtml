@model IEnumerable<WebSite.Models.Yorum>

@{ 
    Layout = null;
}
<div class="row" >
    <div class="col-lg-12">
        <ul>
            @foreach (var item in Model)
            {
                <li>@item.Icerik</li>
            }
        </ul>
    </div>
</div>